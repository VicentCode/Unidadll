<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el Chip está presionado -->
    <item android:state_pressed="true" android:drawable="@color/cyan" />

    <!-- Estado cuando el Chip está seleccionado -->
    <item android:state_selected="true" android:drawable="@color/design_default_color_primary" />

    <!-- Estado predeterminado -->
    <item android:drawable="@color/orange" />
</selector>
